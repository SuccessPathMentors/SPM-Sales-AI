# MIG-002 temporary upload landing zone

Temporary only. Upload exact raw artifacts here without editing or renaming. After byte-identity verification, the migration process reuses the exact Git blobs at their final repository paths and removes this temporary landing zone.

Branch: `migration/rc4-3-3-exact-artifact`
