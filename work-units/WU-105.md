# WU-105 — Golden Intents Optimization

Status: IN_PROGRESS
Version: 0.2
Issue: #60
PR: #61

## Objective
Improve the accuracy, answer quality, next-step quality, and conversion usefulness of a bounded set of high-value/high-risk existing intents (the **Golden Intents**) without changing the locked 62-intent taxonomy, business truth, action permissions, or Production directly.

## Current implementation checkpoint
WU-105 V1 has now frozen a 13-intent Golden manifest, contract/schema tests, and a deterministic inactive STAGING candidate builder. The corrected WU-105 candidate is built **only on the current deterministic WU-104 candidate**, not directly on Production.

Current offline evidence:
- Golden manifest: `contracts/WU105_GOLDEN_INTENTS_V1.json`
- Golden manifest count: `13`
- WU-104 inherited upstream SHA-256: `d0c2ad10c3455b435868a8b7d4c874d31d27ae35e64844d62713d1a5ba74e45f`
- WU-104 inherited node count: `124`
- WU-105 corrected candidate SHA-256: `43ac3b2be6ae51b99b16f4e3166e0c9e0e055ccbc0b67d48871346d594415eed`
- WU-105 candidate node count: `125`
- Corrected CI run: `33650791726` — PASS
- Evidence: `evidence/wu105/offline-contract-candidate.md`

The earlier 115-node candidate built directly from Production is superseded and must never be deployed.

## Why this unit exists
WU-104 hardened short-query and ambiguity behavior. WU-105 now targets the most important existing intent journeys so that the agent is not merely classifying correctly, but answering directly, preserving context, asking the smallest useful next question, and moving the customer toward the correct safe next step.

This is an optimization unit, not a taxonomy redesign.

## Upstream inputs
- WU-104 — Short Query & Ambiguity UX — **required inherited runtime behavior**. Current deterministic candidate is consumed read-only by WU-105.
- WU-103 — Knowledge Maintenance Loop — LOCKED.
- WU-102 — Unanswered Question Queue — LOCKED.
- WU-101 — Conversation Analytics — locked/baselined behavior.
- RC4.3.3 Production workflow — LIVE_PUBLISHED_VERIFIED / READ ONLY.
- Existing 62-intent taxonomy and runtime aliases — authoritative and unchanged.
- Existing source gates, pricing/policy truth controls, deterministic action gateway, consent/confirmation guards, opt-out/support precedence — authoritative and unchanged.

### WU-104 formal dependency reconciliation
GitHub `main` does not yet contain `work-units/WU-104.lock.md`, and `evidence/wu104/runtime-matrix-coverage.md` still records an in-progress runtime matrix. The owner has performed additional live clarification-response checks, but WU-105 final runtime certification must not silently convert that into a formal GitHub lock.

Therefore:
- WU-105 offline contract/build/test work may proceed.
- WU-105 must preserve and inherit current WU-104 controls exactly.
- WU-105 cannot be final-certified/locked until WU-104 is reconciled or an explicit owner dependency waiver is recorded.

## Depends on
- current WU-104 deterministic candidate inherited exactly;
- formal WU-104 reconciliation before final WU-105 certification.

## Affected systems
- GitHub paths:
  - `work-units/WU-105.md`
  - `contracts/WU105_*`
  - `scripts/wu105/**`
  - `evidence/wu105/**`
  - `n8n/generated/SPM_WU105_STAGING_GOLDEN_INTENTS_CANDIDATE.json`
  - `.github/workflows/wu105-*.yml`
- n8n workflow:
  - new inactive WU-105 STAGING candidate only.
- n8n node ownership:
  - one deterministic response-planning prompt overlay only.
- external integrations:
  - none newly authorized by WU-105.

## Frozen V1 Golden Intent set
1. `subject_inquiry`
2. `pricing`
3. `package_comparison`
4. `price_objection`
5. `free_trial`
6. `trial_details`
7. `teacher_quality`
8. `availability`
9. `schedule_request`
10. `registration`
11. `ready_to_register`
12. `human_handoff`
13. `refund_policy`

Selection basis is bounded to business value, conversion entry, confusion risk, source/action risk, and support resolution. Each intent has an explicit reason in the manifest.

## Optimization contract
For each Golden Intent, the V1 manifest freezes:
- direct answer requirement;
- allowed authoritative source(s);
- minimum context needed before asking a question;
- fields that must not be re-asked when already known;
- preferred next-best-action;
- smallest optional qualifier;
- stop/override conditions;
- forbidden unsupported claims/actions;
- nearest confusion-pair intents;
- WU-104 compatibility rule;
- `irreversible_action_allowed=false`.

## Response quality rules
A Golden Intent response must follow this order unless a locked safety/support rule overrides it:
1. answer the user's current question first;
2. reuse trusted known context;
3. do not repeat information unnecessarily;
4. ask at most one smallest relevant missing qualifier when needed;
5. present one clear next step when useful;
6. do not manufacture urgency, guarantees, discounts, availability, credentials, policy outcomes, or action success.

## Implementation design
WU-105 V1 does not add another LLM or modify classification. It adds one deterministic Code node:

`Build WU96-Aware Sales Agent Prompt -> Apply WU105 Golden Intent Prompt Overlay -> Generate WU92 Sales Agent Response`

The overlay activates only when `classification.spm_intent` belongs to the frozen Golden set. It adds bounded guidance to the existing sales-agent prompt while leaving classifier output, source/business truth, action permission, registration consent guards, WU-104 controls, and downstream deterministic gates authoritative.

## In scope
- frozen 13-intent V1 Golden manifest;
- intent-specific answer/NBA optimization for that bounded set;
- confusion-pair tests between selected intents and nearest existing neighbors;
- no-reask and context-continuity tests;
- direct-answer-before-qualification behavior;
- EN/AR/FR semantic parity for the Golden set;
- compatibility with WU-104 ambiguity handling;
- deterministic telemetry-safe Golden-set metadata;
- inactive STAGING candidate and runtime matrix;
- regression against non-Golden intents and locked upstream controls.

## Out of scope
- adding/removing/renaming intents;
- classifier replacement or second classifier LLM;
- broad dialect expansion (WU-106);
- live human handoff adapter (WU-107);
- WhatsApp staff notification (WU-108);
- KPI dashboard implementation (WU-109);
- global optimization of all intents (WU-110 remains downstream);
- pricing/policy/source-truth changes;
- new external side effects or new business-action permissions;
- direct Production activation.

## Acceptance criteria
- [x] AC-01 A versioned V1 Golden Intent manifest exists with 8–15 existing intents and an explicit selection reason for each.
- [x] AC-02 The 62-intent taxonomy is unchanged in offline candidate construction.
- [x] AC-03 Every Golden Intent has a frozen direct-answer/source/NBA/guardrail contract.
- [ ] AC-04 Golden runtime responses answer the current question before asking a qualifier unless a locked safety/support rule requires otherwise.
- [ ] AC-05 Runtime confirms known grade/subject/location/customer context is not re-asked when already trusted and relevant.
- [x] AC-06 Offline inheritance proves no Golden optimization removes or modifies WU-104 nodes.
- [x] AC-07 Static/candidate tests prove no Golden optimization adds or changes source/business/action permissions.
- [ ] AC-08 Runtime confirms high-risk facts/actions remain authoritative/live-source gated.
- [ ] AC-09 Runtime confusion-pair fixtures show the selected Golden Intent is not incorrectly collapsed into its nearest neighbor.
- [ ] AC-10 Runtime confirms a direct, clearly different current intent overrides stale prior sales context.
- [ ] AC-11 EN/AR/FR equivalent Golden scenarios produce equivalent business meaning and routing.
- [x] AC-12 No additional LLM classifier/re-classifier is introduced.
- [ ] AC-13 Non-Golden intent runtime regression remains compatible outside explicitly documented shared response-planner improvements.
- [ ] AC-14 WU-101 analytics can distinguish Golden Intent direct/clarify/fallback/outcome signals without logging new raw PII/session data.
- [ ] AC-15 WU-102 unresolved-question behavior remains compatible and resolved Golden turns are not falsely queued solely because wording was short/ambiguous earlier in the journey.
- [x] AC-16 Production workflow/artifact remains untouched during offline WU-105 construction; exact Production SHA remains protected.

## Tests/evidence required
### Static / contract — PASS
- Golden manifest schema and allowlist against existing classifier definitions;
- duplicate/unknown intent rejection;
- per-intent answer/source/NBA contract completeness;
- source/action-gate invariants;
- no second classifier/model node;
- exact Production SHA protection;
- exact current WU-104 candidate SHA inheritance;
- all 124 WU-104 nodes preserved;
- WU-105 adds exactly one deterministic Code node;
- candidate remains inactive.

### Behavioral fixtures — NEXT
For every Golden Intent:
- fresh-session clear request;
- same request with relevant trusted context;
- stale/conflicting previous intent;
- short explicit request;
- ambiguous bare fragment where WU-104 must clarify;
- nearest confusion-pair request;
- unsupported-fact/source unavailable path;
- EN/AR/FR semantic equivalents.

### Runtime STAGING — PENDING
- run the complete Golden Intent matrix on a separate inactive WU-105 STAGING workflow;
- verify answer-first behavior;
- verify no-reask behavior;
- verify one-smallest-question behavior;
- verify correct safe next step;
- verify zero unauthorized business writes/actions;
- capture execution IDs and candidate SHA-256.

### Regression — PENDING RUNTIME
- WU-104 short-query/ambiguity controls;
- WU-103 knowledge-maintenance boundaries;
- WU-102 unanswered queue compatibility;
- WU-101 analytics compatibility;
- protected non-Golden sample;
- Production identity/hash unchanged.

## Safety / hard stops
- changing the intent taxonomy;
- building WU-105 directly on Production instead of the current WU-104 upstream candidate;
- deploying the superseded 115-node WU-105 candidate;
- optimizing an intent using invented business truth;
- changing prices/policies/availability/teacher claims without an authoritative source;
- allowing optimization to authorize an irreversible action;
- stale context overriding a clear current request;
- re-asking trusted known fields merely to follow a script;
- using out-of-scope as a generic low-confidence bucket;
- bypassing or dropping WU-104 clarification safeguards;
- adding a second LLM classifier/reviewer layer;
- modifying/activating Production directly.

## Delegate contract
### Owns
- WU-105 manifest/contracts/tests and new inactive WU-105 STAGING candidate.

### May read
- `docs/STATE.yaml`;
- current WU-104 deterministic candidate and evidence;
- locked WU-101 through WU-103 artifacts required for compatibility;
- exact RC4.3.3 Production artifact as read-only root baseline;
- current intent catalog/taxonomy.

### Must not modify
- WU-101/WU-102/WU-103 artifacts;
- WU-104 source artifacts while implementing WU-105;
- RC4.3.3 Production workflow;
- 62-intent taxonomy;
- authoritative pricing/policy/credential/source data.

## DAG
`current WU-104 candidate -> WU-105 manifest/contracts -> corrected inherited candidate -> behavioral fixtures -> separate STAGING runtime -> protected regression -> WU-104 dependency reconciliation -> material review -> owner approval -> lock -> WU-106`

## Exit contract
Worker may return only `READY_FOR_REVIEW` or `BLOCKED`.
The worker cannot self-approve, self-activate Production, or self-lock.

## Final evidence
- Branch: `wu105-planning`
- Current implementation commit: `4bf1c3d18f1bf7d2fd6588a809659e2ce5da7133` plus subsequent evidence updates
- PR: `#61` (draft)
- Corrected CI run: `33650791726` PASS
- WU-104 inherited SHA: `d0c2ad10c3455b435868a8b7d4c874d31d27ae35e64844d62713d1a5ba74e45f`
- WU-105 candidate SHA: `43ac3b2be6ae51b99b16f4e3166e0c9e0e055ccbc0b67d48871346d594415eed`
- n8n staging execution IDs: pending
- Runtime test result: pending
- Review result: pending
- Approval: pending
- Locked artifact/version: pending
