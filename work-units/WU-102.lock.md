# WU-102 — Lock Record

Status: LOCKED
Locked date: 2026-08-31
Issue: #25
Owner approval: APPROVED in ChatGPT project review after final acceptance review.

## Locked implementation identity
- n8n STAGING workflow: `[STAGING] SPM WU102 Unanswered Question Queue Candidate`
- n8n workflow ID: `1kaRBBFVJYbPxvQG`
- healthy candidate SHA-256: `6a429304f114b472429cde20c0687499b0b978567b4b3fb983891884dbba9cf5`
- workflow remains inactive in STAGING.
- Production workflow `CMBMpxX5AqqK2UTn` was not modified or activated.

## Certification evidence
PASS evidence includes:
- schema/static/privacy checks;
- deterministic trigger contract tests;
- WU-101 / RC4.3.3 regression parity outside the approved WU-102 insertion path;
- normal-answer negative control (no queue write);
- human-request queue write/readback;
- PII redact-or-withhold runtime evidence;
- OUT_OF_SCOPE runtime evidence;
- EN/AR/FR equivalent OUT_OF_SCOPE queue semantics;
- controlled queue-sink failure injection proving fail-open customer behavior;
- successful restore to the healthy SHA-locked STAGING candidate and remote read-back.

## Accepted evidence notes
- Manual same-event `Execute step` dedupe replay was blocked by the n8n UI remaining on `Loading data`; idempotency is covered by executable contract tests plus the Sheets `appendOrUpdate` adapter matching on `queue_event_id`.
- Missing/invalid internal queue identity fail-open is covered by static/executable contract evidence rather than a separate manual STAGING failure injection.
- These notes were reviewed and accepted as non-blocking before owner approval.

## Change control
WU-102 is immutable after this lock. Any behavior, schema, trigger, privacy, sink, or identity change requires a new Change Request / versioned work unit, review, tests, approval, and re-lock.

## Next dependency
WU-103 Knowledge Maintenance Loop may now proceed using WU-102 as a locked upstream contract.
