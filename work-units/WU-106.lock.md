# WU-106 — Lock Record

Status: LOCKED
Locked date: 2026-09-03
Issue: #65
PR: #66
Owner approval: APPROVED in ChatGPT project review by explicit Owner command `APPROVE WU-106 LOCK` on 2026-09-03.

## Locked implementation identity
- n8n STAGING workflow: `[STAGING] SPM_WU106_END_TO_END_JOURNEYS_V1`
- n8n workflow ID: `vvHvidUHVxM5wTVT`
- locked final CR: `CR-106-03`
- locked candidate SHA-256: `2e219adbdd612106b782993cbcb2f94da6c0737b250264060b473f12f0fcc81f`
- node count: `141`
- final remote versionId: `a353d744-8e37-4a8b-ada5-5c43bfc5e0fc`
- final exact-lineage candidate CI run before Owner approval: `33779359776` — SUCCESS
- exact-lineage candidate artifact: `wu106-cr10603-candidate`, artifact ID `9902904632`, ZIP digest `sha256:8c7cbf4e3ded9d85249564c9dbbf24d026d690ae42951a17ccb5d761c26c293b`
- permanent Journey Contract CI run before Owner approval: `33779359519` — SUCCESS
- permanent final-candidate STAGING dry-run run before Owner approval: `33779359611` — SUCCESS
- Repository Guard run before Owner approval: `33779359533` — SUCCESS
- STAGING workflow remains inactive (`active=false`).
- Production workflow `CMBMpxX5AqqK2UTn` remained protected and untouched throughout WU-106 certification.

## Locked WU-106 objective
WU-106 locks end-to-end, multi-turn journey orchestration across the existing locked WU-105 conversation stack without changing the authoritative 62-intent taxonomy, locked WU-104 ambiguity/correction behavior, locked WU-105 Golden Intent contracts, business truth, or Production permissions.

Locked orchestration behavior includes:
- reuse compatible trusted context across turns without unnecessary re-asking;
- current clear message overrides stale historical objective;
- explicit single-field correction replaces only the corrected field;
- child/student context switch creates a student-scoped boundary and does not silently merge two children;
- inquiry and action execution remain distinct states;
- availability requires an authoritative live scheduling source;
- booking, registration, refund, and handoff success require actual downstream action success;
- support/complaint/handoff can interrupt sales immediately;
- prior sales context may be suspended and reused only after a new customer signal;
- multilingual language switching does not reset semantic context;
- Production mutation and irreversible actions remain prohibited.

## Locked Golden Journey set
WU-106 certifies these 12 end-to-end journey classes:
1. Discovery → Pricing → Package Comparison
2. Discovery → Pricing → Price Objection → Free Trial
3. Trial Details → Explicit Trial Start → Registration Intake
4. Registration → Availability → Schedule Request
5. Availability → Requested Slot → Safe Alternative
6. Known Context → Single-Field Correction
7. Child / Student Context Switch
8. Sales → Human Handoff
9. Sales → Technical Support / Complaint Interruption
10. Policy Question Inside Active Sales Journey
11. Long Conversation → Stale-Context Override
12. Multilingual Continuity / Deliberate Language Switch

The authoritative 62-intent taxonomy is unchanged. WU-106 did not add, remove, or rename taxonomy intents and did not introduce a second WU-106 LLM/classifier/agent.

## Certified corrections
### CR-106-01 — Initial registration/availability recovery attempt
Introduced a narrow contextual recovery layer for awaited registration values and explicit availability transitions. Live retest showed this was insufficient because it still depended on canonical registration continuation state being present in the next-turn payload.

### CR-106-02 — Durable Registration State / Root Journey Recovery
Resolved the root registration continuation defect by introducing:
- PII-free STAGING Redis control key `spm:staging:regctrl:<session_id>`;
- registration control metadata restoration before classifier/catalog processing when canonical continuation is absent;
- deterministic awaited-field binding even when classifier output is strongly wrong;
- explicit availability wording priority over stale registration/clarification context;
- redundant PII-free registration-control persistence after final registration state is known;
- unchanged canonical WU95 sales-state authority and WU-102 isolation/idempotency controls.

GJ-04 live retest passed after CR-106-02.

### CR-106-03 — Scheduling Context + Alternative Availability
Resolved:
- explicit `Toronto time` being re-asked as a missing timezone;
- alternative-slot wording falling into generic action fallback.

Controls introduced:
- explicit Toronto/Mississauga/Milton time alias normalization to `America/Toronto` when the customer explicitly supplies that city-time wording;
- deterministic requested day/time preference capture;
- alternative-slot recovery into availability intent without silently replacing the requested preference;
- live schedule requirement for alternative slots;
- no invented slot and no false booking claim.

GJ-05 live retest passed after CR-106-03.

## Certification evidence
Final acceptance review: `evidence/wu106/final-acceptance-review.md`.

Owner-observed customer-output certification: **12 / 12 PASS** on the final STAGING lineage.

The live certification covered:
- trusted-context reuse and no-reask behavior;
- pricing/package transitions;
- objection → free-trial transition;
- trial-details vs explicit trial action separation;
- registration continuation across multiple turns;
- registration interruption by explicit availability question;
- schedule request action honesty;
- alternative availability without invented slots;
- single-field correction;
- child/student boundary handling;
- human-handoff interruption;
- technical-support/complaint interruption;
- refund-policy interruption and explicit return to sales;
- stale-context override after a long conversation;
- EN→AR, AR→EN, French continuity, and same-language English multi-turn continuity.

Deterministic coverage additionally includes:
- 12 Golden Journey manifest;
- Journey State / Transition Contract;
- 48-scenario deterministic journey matrix;
- exact locked WU-105 upstream lineage reconstruction;
- CR-106-01 → CR-106-02 → CR-106-03 candidate lineage;
- final candidate identity and node-count verification;
- inactive non-production STAGING deployer dry-run;
- Repository Guard;
- Production hard-stop.

## Accepted evidence scope
Owner-observed screenshots certify customer-visible journey behavior. They do not expose internal `sales_state` directly. Internal state/transition invariants are certified separately by deterministic contracts, exact-lineage CI, topology/readback checks, and STAGING safety controls.

The 48-scenario deterministic matrix is not 48 separate live LLM conversations; it certifies matrix completeness and executable transition/safety behavior. The 12 Owner-observed Golden Journeys provide the live customer-output certification layer.

## Retained non-blocking QA observations
The following observations remain visible for later content/grounding/directness work and are not discarded by the lock:
1. Wording similar to `Our team will coordinate suitable options for you` is broader than necessary and should be tightened later to avoid implying automatic human follow-up.
2. One Grade 10 Physics response mentioned Ontario standards when Ontario was not explicitly supplied in that test session; later grounding/content QA should require curriculum/location language to be source-backed or customer-context-backed.
3. Asking for preferred teaching language was unnecessary for the tested Grade 10 Physics inquiry and should be reviewed under minimal-question/directness discipline.

These observations did not violate the locked WU-106 Golden Journey acceptance contracts and were accepted as non-blocking by the Owner when approving the WU-106 lock.

## Change control
WU-106 is immutable after this lock. Any functional change to multi-turn journey orchestration, trusted-context reuse, stale-context precedence, correction behavior, child/student boundaries, registration durable control, availability/schedule separation, multilingual continuity, support/handoff interruption behavior, action honesty, WU-104/WU-105 inheritance, Redis isolation, or Production boundary requires a versioned Change Request/work unit, executable tests, material review, explicit Owner approval, and re-lock.

This lock authorizes the completed WU-106 STAGING certification only. It does **not** authorize Production activation, Production deployment, or modification of the protected Production workflow.

## Next dependency
WU-107 becomes eligible to continue/start only after this lock record is merged cleanly to `main` with required repository checks passing.
