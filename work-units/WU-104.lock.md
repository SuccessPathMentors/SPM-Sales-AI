# WU-104 — Lock Record

Status: LOCKED
Locked date: 2026-09-02
Issue: #48
Owner approval: APPROVED in ChatGPT project review by explicit Owner command `APPROVE WU-104 LOCK` on 2026-09-02.

## Locked implementation identity
- n8n STAGING workflow: `[STAGING] SPM WU104 Short Query + Ambiguity UX Candidate`
- n8n workflow ID: `Bt3PvOIbFzU0O9gk`
- locked final CR: `CR-104-05`
- locked candidate SHA-256: `32721cae2b09531d8f4860373c37911ace9e95b6818babdca880fa08ef1b7bc9`
- node count: `126`
- final remote versionId: `4718dbfd-e628-4215-a9c5-97094b830e30`
- final deployment/readback evidence: GitHub Actions run `33663124381` — PASS
- STAGING workflow remains inactive (`active=false`).
- Production workflow `CMBMpxX5AqqK2UTn` remained protected and untouched throughout certification.

## Locked behavior
WU-104 locks the following customer-facing and deterministic behavior:
- shortness alone does not imply ambiguity;
- clear short semantic requests remain direct when one intent is clear;
- bare grade/subject/location/day/time replies bind only to one compatible awaited context;
- current-message corrections override stale state;
- clarification asks exactly one compact question and does not re-ask known fields;
- clarification language parity is maintained across EN/AR/FR;
- clarification never grants irreversible/business-action permission;
- repeated unresolved ambiguity is capped after two clarification attempts and transitions to safe fallback/human-help;
- stale unrelated context cannot override a clear current message;
- WU-101/WU-102 analytics/unanswered-queue compatibility remains intact;
- WU-104 decision/evidence state adds no raw customer/session/contact/secret persistence.

## Certified corrections
- `CR-104-03`: final-response question whitelist fallback for `grade|subject|location|day|time`, with WU-104 clarification prompts explicitly blocked from becoming false awaited context.
- `CR-104-04`: single-student validated grade/subject reconciliation, preserving known subject during grade correction while guarding multi-student and unvalidated-value cases.
- `CR-104-05`: terse trial-information question guard so `free trial?` and EN/AR/FR equivalents resolve to trial information rather than immediately entering lead intake, while explicit start/book/request trial phrases remain `free_trial`.

## Certification evidence
Final review in `work-units/WU-104.review.md` assessed AC-01 through AC-14 PASS for STAGING certification. Evidence includes:
- contract/core/candidate tests PASS;
- awaited-context integration and executable-signal tests PASS;
- known-slot reconciliation tests PASS;
- CR-104-05 short-trial executable EN/AR/FR tests PASS;
- inactive non-production deploy and remote readback PASS;
- runtime PASS for direct short pricing, fresh bare-subject ambiguity, awaited subject/grade/location/day/time binding, correction precedence, fresh Yes/No safety guard, Arabic/French parity, clarification loop cap, stale-context precedence, `free trial?`, `human?`, and long-form pricing/scheduling/support/registration regression;
- no second LLM/classifier introduced by WU-104;
- Production and locked WU-101/WU-102/WU-103 workflow IDs protected by deployment hard-stops.

## Accepted evidence limitation
A positive live contextual Yes/No confirmation case was not reached because the attempted registration flow asked for `parent_name` rather than presenting a binary confirmation question. That attempt was correctly marked NOT SCORED. The exact positive confirmation behavior is covered deterministically in `scripts/wu104/test_core.py`: with `conversion.awaiting_field=final_confirmation` and the registration confirmation guard active, `yes` and `no` bind deterministically while `irreversible_action_allowed=false`. Fresh-session `Yes` was separately exercised live and correctly clarified instead of inventing consent/action. Owner accepted the final review and explicitly approved lock with this limitation documented.

## Change control
WU-104 is immutable after this lock. Any functional change to short-query classification behavior, awaited-context persistence/binding, correction precedence, clarification language/content, loop-cap behavior, trial-question remapping, privacy/evidence fields, WU-101/WU-102 compatibility, or Production boundary requires a versioned Change Request/work unit, executable tests, review, explicit Owner approval, and re-lock.

This lock authorizes the completed WU-104 STAGING certification only. It does **not** authorize Production activation or deployment.

## Next dependency
WU-105 is now eligible to start using WU-104 as a locked upstream work unit.
