# WU-103 — Lock Record

Status: LOCKED
Locked date: 2026-09-01
Issue: #30
Owner approval: APPROVED in ChatGPT project review after final acceptance review.

## Locked implementation identity
- n8n STAGING workflow: `[STAGING] SPM WU103 Knowledge Maintenance STAGING Candidate`
- n8n workflow ID: `5COEoxXjk8AvuGBa`
- locked hardened candidate SHA-256: `0d53579c9bc93a0a440b8f8c5e9e6545d3dd4b11399116eea57efee6b9fff746`
- maintenance workflow remains inactive / Manual Trigger STAGING only.
- WU-103 write targets are limited to `WU103_CHANGE_LEDGER_STAGING` (`sheetId=2026103001`) and `WU103_KB_SHADOW_STAGING` (`sheetId=2026103002`).
- Production workflow `CMBMpxX5AqqK2UTn` and canonical Production KB families remain outside the WU-103 STAGING write boundary.

## Locked contract corrections
- CR-103-01: `candidate_payload_json` stores curated/sanitized family-allowlisted knowledge payload; it is hash-bound to `candidate_payload_sha256` and is not an automatic copy of queue text.
- CR-103-02: `regression_payload_sha256` binds Regression PASS to the exact tested candidate payload; payload edits invalidate prior PASS eligibility until regression is rerun.

## Certification evidence
Final review in `work-units/WU-103.review.md` assessed AC-01 through AC-19 PASS for STAGING certification. Evidence includes:
- strict ledger/shadow schemas, family adapters, privacy/secret exclusions, and deterministic candidate/payload/fingerprint hashes;
- human approval, business-truth approval, regression evidence, and release approval hard gates;
- controlled STAGING ADD publication/readback and idempotent retry;
- controlled STAGING UPDATE with v1→v2 supersession and preserved history;
- first managed UPDATE of canonical legacy `FALLBACKS/FB-003` using `LEGACY_UNVERSIONED` plus exact base fingerprint;
- live stale-base rejection with no stale v3 publication;
- Regression FAIL blocking publish;
- Regression PASS without release approval blocking publish;
- exact regression-payload binding with evidence SHA and regression case IDs;
- privacy flags and provenance lineage using `source_queue_event_id` without raw customer/session/contact identity;
- proof that canonical Production KB remained unchanged during certification.

## Accepted evidence notes
- Earliest ADD/v2 certification rows are historical pre-final-hardening fixtures. They retain empty regression-case IDs and one stale reviewer-note string and are not used as the authoritative proof of the hardened regression gate. Runtime Test #6 is the authoritative final regression-evidence proof.
- A live duplicate-ACTIVE-shadow corruption was intentionally not injected into the shared STAGING history solely to reproduce `BASE_RECORD_NOT_UNIQUE`; that rejection branch is deterministically covered by executable core/static tests, while the adjacent stale-base branch was exercised live.
- These limitations were explicitly reviewed and accepted as non-blocking before Owner approval.

## Change control
WU-103 is immutable after this lock. Any change to its ledger/shadow schema, approval boundaries, family adapters, payload/regression binding, versioning/supersession semantics, stale-base behavior, privacy/provenance rules, STAGING publisher, or Production-publish boundary requires a versioned Change Request/work unit, tests, review, explicit approval, and re-lock.

This lock authorizes the completed WU-103 STAGING certification only. It does **not** authorize automatic or direct Production KB publication.

## Next dependency
WU-104 Short Query & Ambiguity UX may now proceed using WU-103 as a locked upstream work unit.
